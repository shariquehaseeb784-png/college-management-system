const express=require("express");const Fee=require("../models/Fee");const {authenticate,authorize}=require("../middleware/auth");const router=express.Router();router.use(authenticate);router.get("/",authorize("admin"),async(req,res)=>res.json(await Fee.find().populate("student","name rollNumber course").sort({dueDate:1})));router.post("/",authorize("admin"),async(req,res)=>{try{const x=await Fee.create(req.body);res.status(201).json(x);}catch(e){res.status(400).json({message:e.message});}});router.put("/:id",authorize("admin"),async(req,res)=>res.json(await Fee.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true})));router.delete("/:id",authorize("admin"),async(req,res)=>{await Fee.findByIdAndDelete(req.params.id);res.json({message:"Fee deleted"});});router.get("/my",authorize("student"),async(req,res)=>res.json(await Fee.find({student:req.user.studentId}).sort({dueDate:1})));module.exports=router;