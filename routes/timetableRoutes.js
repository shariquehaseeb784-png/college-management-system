const express=require("express");const Timetable=require("../models/Timetable");const {authenticate,authorize}=require("../middleware/auth");const router=express.Router();router.use(authenticate);router.get("/",async(req,res)=>res.json(await Timetable.find().populate("subject","name code").populate("faculty","name employeeId").sort({day:1,startTime:1})));router.post("/",authorize("admin"),async(req,res)=>res.status(201).json(await Timetable.create(req.body)));router.put("/:id",authorize("admin"),async(req,res)=>res.json(await Timetable.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true})));router.delete("/:id",authorize("admin"),async(req,res)=>{await Timetable.findByIdAndDelete(req.params.id);res.json({message:"Timetable entry deleted"});});module.exports=router;