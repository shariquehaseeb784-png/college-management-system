const express=require("express");const Subject=require("../models/Subject");const {authenticate,authorize}=require("../middleware/auth");const router=express.Router();router.use(authenticate);router.get("/",async(req,res)=>res.json(await Subject.find().populate("department","name code").sort({semester:1,name:1})));router.post("/",authorize("admin"),async(req,res)=>{try{res.status(201).json(await Subject.create(req.body));}catch(e){res.status(400).json({message:e.message});}});router.put("/:id",authorize("admin"),async(req,res)=>res.json(await Subject.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true})));router.delete("/:id",authorize("admin"),async(req,res)=>{await Subject.findByIdAndDelete(req.params.id);res.json({message:"Subject deleted"});});module.exports=router;