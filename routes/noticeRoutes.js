const express=require("express");const Notice=require("../models/Notice");const {authenticate,authorize}=require("../middleware/auth");const router=express.Router();router.use(authenticate);router.get("/",async(req,res)=>res.json(await Notice.find({active:true}).sort({publishDate:-1,createdAt:-1})));router.post("/",authorize("admin"),async(req,res)=>res.status(201).json(await Notice.create(req.body)));router.put("/:id",authorize("admin"),async(req,res)=>res.json(await Notice.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true})));router.delete("/:id",authorize("admin"),async(req,res)=>{await Notice.findByIdAndDelete(req.params.id);res.json({message:"Notice deleted"});});module.exports=router;