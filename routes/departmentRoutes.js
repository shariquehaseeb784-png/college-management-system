const express=require("express");const Department=require("../models/Department");const {authenticate,authorize}=require("../middleware/auth");const router=express.Router();router.use(authenticate);router.get("/",async(req,res)=>res.json(await Department.find().sort({name:1})));router.post("/",authorize("admin"),async(req,res)=>{try{res.status(201).json(await Department.create(req.body));}catch(e){res.status(400).json({message:e.message});}});router.put("/:id",authorize("admin"),async(req,res)=>{res.json(await Department.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true}));});router.delete("/:id",authorize("admin"),async(req,res)=>{await Department.findByIdAndDelete(req.params.id);res.json({message:"Department deleted"});});module.exports=router;