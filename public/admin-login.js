const form=document.getElementById("adminLoginForm");const error=document.getElementById("error");const existingToken=localStorage.getItem("cmsToken");const existingUser=JSON.parse(localStorage.getItem("cmsUser")||"null");if(existingToken&&existingUser?.role==="admin")location.href="dashboard.html";form.addEventListener("submit",async e=>{e.preventDefault();error.textContent="";try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:document.getElementById("email").value,password:document.getElementById("password").value})});const d=await r.json();if(!r.ok)throw new Error(d.message||"Login failed");if(d.user.role!=="admin")throw new Error("This page is only for administrators");localStorage.setItem("cmsToken",d.token);localStorage.setItem("cmsUser",JSON.stringify(d.user));location.href="dashboard.html"}catch(e){error.textContent=e.message}});
const togglePassword=document.getElementById("togglePassword");if(togglePassword){togglePassword.addEventListener("click",()=>{const p=document.getElementById("password");const show=p.type==="password";p.type=show?"text":"password";togglePassword.textContent=show?"🙈 Hide":"👁️ Show";});}